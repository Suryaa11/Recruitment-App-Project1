apiVersion: v1
kind: ConfigMap
metadata:
  name: recruitment-config
  namespace: surya-ns
data:
  NODE_ENV: production
  PORT: "3000"
  MONGO_URI: mongodb://rootuser:rootpassword@mongo-recruitment-service:27017/hr_system?authSource=admin
  DB_NAME: hr_system
  MONGO_INITDB_USERNAME: rootuser
  MONGO_INITDB_PASSWORD: rootpassword
  MONGO_USERNAME: rootuser
  MONGO_PASSWORD: rootpassword
  DEFAULT_ADMIN_ID: project
  DEFAULT_ADMIN_PASSWORD: project123
  LOG_LEVEL: info